<%= link_to 'New Category', new_category_path %>

<table>
  <thead>
    <th>Name</th>
  <thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
      <tr>
    <% end %>
  </tbody>
</table>

